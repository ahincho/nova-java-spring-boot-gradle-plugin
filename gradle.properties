version=0.1.0-SNAPSHOT
group=pe.edu.nova.java

# NOVA-SEMVER-23: Habilitar Gradle Local Build Cache
org.gradle.caching=true

# NOVA-SEMVER-24: Habilitar Configuration Cache (Gradle 8.6+).
# Local override for this repo: Spring Boot 4.1.0 + tomcat-embed-* strictly(11.0.24)
# triggers a Gradle 9 compileJava.classpath config-cache serialization bug.
# Disabling here keeps the OWASP gate green (12 CVEs -> 0) until the upstream
# Gradle / Spring Boot Gradle plugin bug is resolved.
org.gradle.configuration-cache=false